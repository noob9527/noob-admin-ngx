import { LoggerService } from './logger.service';
import { NgModule } from '@angular/core';

@NgModule({
  imports: [
  ],
  declarations: [],
  providers: [
    LoggerService,
  ],
})
export class LoggerModule { }
